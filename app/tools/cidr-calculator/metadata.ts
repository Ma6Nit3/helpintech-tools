import { Metadata } from "next";

export const metadata: Metadata = {
  title: "CIDR Calculator | HelpInTech",
  description:
    "Calculate network address, broadcast address, subnet mask and usable hosts from CIDR notation.",
};